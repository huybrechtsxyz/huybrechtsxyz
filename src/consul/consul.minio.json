{
  "service": {
    "name": "minio",
    "tags": [ "data", "admin" ],
    "port": 9001,
    "checks": [
      {
        "id": "minio-http-check",
        "name": "Minio 4 HTTP Health Check",
        "http": "http://minio:9001",
        "interval": "10s",
        "timeout": "1s"
      }
    ]
  }
}
