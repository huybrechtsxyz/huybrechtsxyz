<script>
    function linkFormatter(value, row, index) {

        const valuesArray = Object.values(row);
        const valuesString = valuesArray.join(', ');
        const keysArray = Object.keys(row);
        const keysString = keysArray.join(', ');

        return `<a href="/Features/Project/Component/Edit?id=${value}" class="btn btn-icon btn-primary">
                    <i class="bi bi-pencil-square"></i>
                </a>
                <a href="/Features/Project/Component/Delete?id=${value}" class="btn btn-icon btn-secondary">
                    <i class="bi bi-trash2-fill"></i>
                </a>
                <a href="/Features/Project/Component/Create?projectDesignId=${row.ProjectDesignId}&parentId=${row._Id_data}" class="btn btn-info">
                    <i class="bi bi-layers"></i>
                    <span class="vr"></span>
                    Components
                </a>
                <div>${value}</div>
                <div>${keysArray}</div>
                <div>${valuesString}</div>`;
    }
</script>



<td>
    <a asp-page="Edit" asp-route-id="@Model.Id" class="btn btn-icon btn-primary"><i class="bi bi-pencil-square"></i></a>
    <a asp-page="Delete" asp-route-id="@Model.Id" class="btn btn-icon btn-secondary"><i class="bi bi-trash2-fill"></i></a>
    <a asp-page="/Features/Project/Component/Create" asp-route-projectDesignId="@Model.ProjectDesignId" asp-route-parentId="@Model.Id" class="btn btn-info">
        <i class="bi bi-layers"></i>
        <span class="vr"></span>
        Components
    </a>
    <a href="http://www.duckduckgo.com" class="btn btn-primary">Link Text</a>
</td>


columns: [
                    { title: "Name", field: "name", sorter: "string" },
                    { title: "Component Level", field: "componentLevel", sorter: "string" },
                    { title: "Variant Type", field: "variantType", sorter: "string" },
                    { title: "Source", field: "source", sorter: "string" },
                    {
                        title: "Actions", field: "actions", headerSort: false, width: 150, align: "center", formatter: function (cell, formatterParams) {
                            var id = cell.getRow().getData().id; // Get the id of the row
                            return `
                                <button class="btn btn-icon btn-primary edit-btn" data-id="${id}"><i class="bi bi-pencil-square"></i></button>
                                <button class="btn btn-icon btn-secondary delete-btn" data-id="${id}"><i class="bi bi-trash2-fill"></i></button>
                                <button class="btn btn-icon btn-primary add-btn" data-id="${id}"><i class="bi bi-plus-circle"></i></button>
                                <button class="btn btn-icon btn-info measure-btn" data-id="${id}"><i class="bi bi-layout-text-window"></i></button>
                            `;
                        }
                    }
                ],