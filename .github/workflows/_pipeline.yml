# Build and Deploy Jobs
name: _BuildAndDeploy

on:
  workflow_call:
    inputs:
      environment:
        required: true
        type: string
      initialize:
        required: true
        type: string
      rebuild:
        required: true
        type: string
    secrets:
      BITWARDEN_TOKEN:
        required: true

jobs:
  terraform:
    name: Terraform Swarm Cluster
    if: inputs.initialize == 'true'
    uses: ./.github/workflows/_task-terraform.yml
    with:
      environment: ${{ inputs.environment }}
    # Ensure the secret is defined in the referenced workflow or remove this section if not required
    secrets: inherit
  
  initialize:
    name: Initialize Swarm Cluster
    needs: [terraform]
    if: inputs.initialize == 'true'
    uses: ./.github/workflows/_task-initialize.yml
    with:
      environment: ${{ inputs.environment }}
      tf_output_json: ${{ needs.terraform.outputs.tf_output_json }}
    secrets: inherit
